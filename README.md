# door_cardreader

```shell
./build-and-run-local.sh # local build, requires submodules checked out and libraries installed
./build-and-run-docker.sh # containerized build, w/o checking out submodules locally
# See calling the commands within the scripts for details
# e.g.:
./read --help # show usage
# Example usage:
./read keys/app1.r11.key 11 0x112233 11

```